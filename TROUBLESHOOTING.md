# Решение проблем с Android Studio

## 🚨 Проблема: "Unable to delete directory"

Эта ошибка возникает когда файлы проекта заблокированы процессами Android Studio.

### 🛠️ Способы решения:

#### Способ 1: Перезапуск Android Studio (рекомендуется)
1. **Закройте Android Studio полностью**
2. **Откройте Диспетчер задач** (Ctrl+Shift+Esc)
3. **Найдите и завершите все процессы** `java.exe` связанные с Android Studio
4. **Подождите 10-15 секунд**
5. **Откройте Android Studio снова**
6. **Откройте проект** и попробуйте запустить

#### Способ 2: Очистка через Gradle
1. **Откройте командную строку** в папке проекта
2. **Выполните команды:**
   ```bash
   cd "D:\DK_PROJECTS\_LIFE\shop_list_android_app\dev\android_studio_project"
   $env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
   .\gradlew clean
   .\gradlew build
   ```

#### Способ 3: Перезагрузка компьютера
Самый надежный способ если ничего не помогает:
1. **Сохраните все изменения в коде**
2. **Перезагрузите компьютер**
3. **Откройте Android Studio и проект**

#### Способ 4: Удаление папки build вручную
1. **Закройте Android Studio**
2. **Удалите папку** `android_studio_project/app/build`
3. **Откройте Android Studio**
4. **Проект пересоберется автоматически**

### ✅ Проверка результата

После решения проблемы проверьте:
- APK файл должен быть в: `app/build/outputs/apk/debug/app-debug.apk`
- Приложение должно запускаться на эмуляторе или устройстве

### 📱 Как установить приложение

#### Через ADB (если установлено):
```bash
adb install app-debug.apk
```

#### Через эмулятор:
1. **Запустите эмулятор Android**
2. **Перетащите APK файл** в окно эмулятора
3. **Приложение установится автоматически**

#### Через Android Studio:
1. **Подключите устройство** или запустите эмулятор
2. **Нажмите зеленую кнопку Run (▶️)**
3. **Выберите устройство** для установки

### 🚨 **ВАЖНО: Если приложение падает при запуске**

**Проблема:** Приложение показывает пустой экран и завершается с ошибкой "stopping"

**Решение:** Исправлена тема приложения в файлах:
- `app/src/main/res/values/themes.xml`
- `app/src/main/res/values-night/themes.xml`

**Изменения:** Заменена тема с `Theme.MaterialComponents.DayNight.DarkActionBar` на `Theme.MaterialComponents.DayNight.NoActionBar`

**После исправления приложение должно запускаться корректно и показывать список продуктов.**

### 🎯 Готовое приложение

**APK файл уже создан и готов к использованию:**
```
D:\DK_PROJECTS\_LIFE\shop_list_android_app\dev\android_studio_project\app\build\outputs\apk\debug\app-debug.apk
```

**Функционал приложения:**
- ✅ 25 продуктов в алфавитном порядке
- ✅ Переключатели ON/OFF для каждого продукта
- ✅ Современный Material Design интерфейс
- ✅ Сохранение состояния между запусками

### 📞 Если проблема осталась

Если ни один из способов не помог:
1. **Проверьте антивирус** - он может блокировать файлы
2. **Проверьте права доступа** к папке проекта
3. **Попробуйте создать новый проект** в другой папке

---

**Приложение готово к использованию!** 🎉
