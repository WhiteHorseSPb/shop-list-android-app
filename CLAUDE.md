# 🤖 CLAUDE.md - Руководство для AI-ассистентов

> **Цель документа:** Предоставить AI-ассистентам comprehensive понимание проекта "Срочно купить" для эффективной работы с кодовой базой.

**Версия:** 1.7
**Дата обновления:** 2025-11-18
**Тип проекта:** Android приложение (Kotlin)
**Архитектура:** Упрощенная слоистая архитектура без MVVM

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура и структура](#архитектура-и-структура)
3. [Ключевые концепции](#ключевые-концепции)
4. [Технический стек](#технический-стек)
5. [Соглашения по коду](#соглашения-по-коду)
6. [Рабочие процессы](#рабочие-процессы)
7. [Git правила](#git-правила)
8. [Частые задачи](#частые-задачи)
9. [Подводные камни](#подводные-камни)
10. [Тестирование](#тестирование)
11. [Сборка и развертывание](#сборка-и-развертывание)

---

## 🎯 Обзор проекта

### Описание
**"Срочно купить"** (DK Shopping List) — Android приложение для управления списком покупок с трехуровневой приоритизацией и визуальным выделением срочных товаров.

### Основной функционал
- **Трехуровневая группировка товаров** (Срочно/Важно/Остальное)
- **Управление срочностью** через клик по звездочке
- **Переключение статуса покупки** через Material Switch
- **Автоматическое сохранение** в SharedPreferences
- **Swipe-to-delete** для удаления товаров
- **Адаптивная двухколоночная сетка** с GridLayoutManager

### Ключевые особенности
- ✅ Оптимизация для устройств Xiaomi/MIUI
- ✅ DiffUtil для эффективного обновления RecyclerView
- ✅ Обратная совместимость данных (миграция старого формата)
- ✅ Детальное логирование для отладки
- ✅ Material Design 3 компоненты
- ✅ Поддержка темной темы

### Package структура
```
com.example.shoppinglist
├── MainActivity.kt         # UI и взаимодействие
├── Product.kt              # Модель данных + группировка
├── ProductAdapter.kt       # RecyclerView адаптер
├── ProductManager.kt       # Бизнес-логика
└── DataManager.kt          # Персистентность данных
```

---

## 🏗️ Архитектура и структура

### Архитектурная модель

```
┌────────────────────────────────────────────────────────┐
│                  PRESENTATION LAYER                    │
│  ┌──────────────────────────────────────────────────┐  │
│  │  MainActivity.kt (240 строк)                     │  │
│  │  • RecyclerView + GridLayoutManager (2 колонки)  │  │
│  │  • SwipeToDelete через ItemTouchHelper           │  │
│  │  • FloatingActionButton для добавления           │  │
│  │  • Dialog для ввода товара                       │  │
│  │  • Lifecycle-aware сохранение данных             │  │
│  └──────────────────────────────────────────────────┘  │
│                          ↕                             │
│  ┌──────────────────────────────────────────────────┐  │
│  │  ProductAdapter.kt (343 строки)                  │  │
│  │  • 2 типа ViewHolder (Header + Product)          │  │
│  │  • DiffUtil с payloads для оптимизации           │  │
│  │  • Специальная логика для MIUI                   │  │
│  │  • Принудительное обновление визуальных элементов│  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
                          ↕
┌────────────────────────────────────────────────────────┐
│                 BUSINESS LOGIC LAYER                   │
│  ┌──────────────────────────────────────────────────┐  │
│  │  ProductManager.kt (115 строк)                   │  │
│  │  • Управление списком продуктов                  │  │
│  │  • Группировка по ProductGroup enum              │  │
│  │  • Сортировка внутри групп (alphabetical)        │  │
│  │  • Валидация дубликатов                          │  │
│  │  • Callbacks для уведомления UI                  │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
                          ↕
┌────────────────────────────────────────────────────────┐
│                    DATA LAYER                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  DataManager.kt (107 строк)                      │  │
│  │  • SharedPreferences для хранения                │  │
│  │  • Gson для JSON сериализации                    │  │
│  │  • Backward compatibility с старым форматом      │  │
│  │  • Автоматическая миграция данных                │  │
│  └──────────────────────────────────────────────────┘  │
│                          ↕                             │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Product.kt (38 строк) - Data Model              │  │
│  │  data class Product(                             │  │
│  │      val name: String,                           │  │
│  │      var needsToBuy: Boolean = false,            │  │
│  │      var isUrgent: Boolean = false               │  │
│  │  ) {                                              │  │
│  │      fun getGroup(): ProductGroup                │  │
│  │  }                                                │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

### Файловая структура проекта

```
shop-list-android-app/
│
├── app/                                    # Основной модуль приложения
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/example/shoppinglist/
│   │   │   │   ├── MainActivity.kt         # Главная Activity
│   │   │   │   ├── Product.kt              # Модель данных + enum ProductGroup
│   │   │   │   ├── ProductAdapter.kt       # RecyclerView адаптер
│   │   │   │   ├── ProductManager.kt       # Бизнес-логика
│   │   │   │   └── DataManager.kt          # Управление данными
│   │   │   │
│   │   │   ├── res/
│   │   │   │   ├── layout/
│   │   │   │   │   ├── activity_main.xml           # Главный экран
│   │   │   │   │   ├── dialog_add_product.xml      # Диалог добавления
│   │   │   │   │   ├── product_item.xml            # Карточка товара
│   │   │   │   │   └── group_header_item.xml       # Заголовок группы
│   │   │   │   │
│   │   │   │   ├── drawable/
│   │   │   │   │   ├── urgent_background_rounded.xml      # Розовый фон (Срочно)
│   │   │   │   │   ├── product_item_background_unified.xml # Градиент (Важно)
│   │   │   │   │   ├── product_item_background_white.xml  # Белый фон (Остальное)
│   │   │   │   │   ├── ic_star_filled.xml                  # Звездочка заполненная
│   │   │   │   │   ├── ic_star_outline.xml                 # Звездочка контур
│   │   │   │   │   └── ... (всего 11 файлов)
│   │   │   │   │
│   │   │   │   ├── values/
│   │   │   │   │   ├── colors.xml              # Цветовая палитра
│   │   │   │   │   ├── strings.xml             # Текстовые ресурсы
│   │   │   │   │   └── themes.xml              # Тема приложения
│   │   │   │   │
│   │   │   │   ├── values-night/               # Темная тема
│   │   │   │   │   └── themes.xml
│   │   │   │   │
│   │   │   │   └── mipmap-*/                   # Иконки приложения
│   │   │   │
│   │   │   └── AndroidManifest.xml
│   │   │
│   │   ├── test/                               # Unit тесты
│   │   │   └── java/com/example/shoppinglist/
│   │   │       └── ExampleUnitTest.kt
│   │   │
│   │   └── androidTest/                        # Instrumented тесты
│   │       └── java/com/example/shoppinglist/
│   │           └── ExampleInstrumentedTest.kt
│   │
│   ├── build.gradle                            # Gradle конфигурация модуля
│   └── proguard-rules.pro                      # ProGuard правила
│
├── gradle/
│   ├── wrapper/
│   │   ├── gradle-wrapper.jar
│   │   └── gradle-wrapper.properties
│   └── libs.versions.toml                      # Version Catalog
│
├── releases/                                   # Готовые APK файлы
│   ├── shopping-list-v1.5-successful-build-*.apk
│   └── README.md
│
├── .idea/                                      # Android Studio настройки
├── .vscode/                                    # VS Code настройки
│   └── settings.json
│
├── build.gradle                                # Корневой build.gradle
├── settings.gradle                             # Project settings
├── gradle.properties                           # Gradle properties
├── gradlew                                     # Gradle wrapper (Unix)
├── gradlew.bat                                 # Gradle wrapper (Windows)
│
├── README.md                                   # Основная документация (русский)
├── CLAUDE.md                                   # Этот файл - для AI ассистентов
├── GRADLE_JAVA_FIX.md                          # Решение проблем сборки
├── PROJECT_OPTIMIZATION_COMPLETE.md            # История оптимизации
├── URGENCY_VISUAL_FIX_INSTRUCTION.md           # Инструкции по визуализации
├── OTHER_GROUP_VISUAL_FIX_INSTRUCTION.md       # Фиксы визуализации "Остальное"
└── ... (другие .md файлы с историей проекта)
```

---

## 🔑 Ключевые концепции

### 1. Трехуровневая группировка товаров

**Это КРИТИЧЕСКИ ВАЖНАЯ концепция приложения!**

```kotlin
enum class ProductGroup {
    URGENT_TO_BUY,    // 🔥 СРОЧНО
    TO_BUY,           // 🛒 ВАЖНО
    OTHER             // 📋 ОСТАЛЬНОЕ
}
```

**Логика определения группы:**

```kotlin
fun getGroup(): ProductGroup {
    return when {
        isUrgent && needsToBuy -> ProductGroup.URGENT_TO_BUY
        !isUrgent && needsToBuy -> ProductGroup.TO_BUY
        !isUrgent && !needsToBuy -> ProductGroup.OTHER
        else -> ProductGroup.OTHER  // isUrgent && !needsToBuy
    }
}
```

**Таблица состояний:**

| isUrgent | needsToBuy | Группа | Визуализация | Описание |
|----------|------------|--------|--------------|----------|
| `true`   | `true`     | **URGENT_TO_BUY** | 🔥 Розовый фон + яркая звезда | Срочная покупка |
| `false`  | `true`     | **TO_BUY** | 🛒 Градиент + полупрозрачная звезда | Обычная покупка |
| `false`  | `false`    | **OTHER** | 📋 Белый фон + бледная звезда | Купленное/не нужное |
| `true`   | `false`    | **OTHER** | 📋 Белый фон + бледная звезда | Невалидное состояние* |

**\*Примечание:** Состояние `isUrgent=true && needsToBuy=false` считается невалидным. Если пользователь переключает `needsToBuy` в `false`, `isUrgent` автоматически сбрасывается в `false`. Это реализовано в `ProductManager.updateProductPurchaseStatus()`.

### 2. Визуальная дифференциация групп

**Фоны карточек:**
- `urgent_background_rounded.xml` — Розовый градиент для URGENT_TO_BUY
- `product_item_background_unified.xml` — Голубой градиент для TO_BUY
- `product_item_background_white.xml` — Белый фон для OTHER

**Звездочка срочности (urgentStar):**
- **URGENT_TO_BUY:** `alpha = 1.0f`, полностью видна
- **TO_BUY:** `alpha = 0.3f`, полупрозрачная
- **OTHER:** `alpha = 0.1f`, почти невидима

**⚠️ ВАЖНО:** Фон карточки определяется по `product.getGroup()`, а НЕ напрямую по флагу `isUrgent`. Это ключевое отличие, которое устраняет баг #13.

### 3. Автоматическое управление состояниями

**Правила:**

1. **При клике на звездочку (toggle urgency):**
   ```kotlin
   if (!product.isUrgent) {
       product.isUrgent = true
       product.needsToBuy = true  // Автоматически включаем
   } else {
       product.isUrgent = false
   }
   ```

2. **При переключении needsToBuy в false:**
   ```kotlin
   if (!needsToBuy) {
       product.isUrgent = false  // Сбрасываем срочность
   }
   ```

3. **При переключении needsToBuy в true:**
   ```kotlin
   // isUrgent остается без изменений
   ```

### 4. Сортировка и порядок отображения

**Порядок групп (сверху вниз):**
1. 🔥 СРОЧНО (URGENT_TO_BUY)
2. 🛒 ВАЖНО (TO_BUY)
3. 📋 ОСТАЛЬНОЕ (OTHER)

**Сортировка внутри группы:**
- Алфавитная сортировка по `name` (case-insensitive через `sortedBy { it.name }`)

**Структура списка для RecyclerView:**
```
[
    "🔥 СРОЧНО",              // Заголовок группы (TYPE_GROUP_HEADER)
    Product("Молоко"),        // Товары (TYPE_PRODUCT)
    Product("Хлеб"),
    "🛒 ВАЖНО",               // Заголовок группы
    Product("Сахар"),
    Product("Чай"),
    "📋 ОСТАЛЬНОЕ",           // Заголовок группы
    Product("Кофе"),
]
```

### 5. Персистентность данных

**Формат хранения:** JSON через Gson в SharedPreferences

**Ключ:** `"products_list_v2"`

**Структура JSON:**
```json
[
    {
        "name": "Молоко",
        "needsToBuy": true,
        "isUrgent": true
    },
    {
        "name": "Хлеб",
        "needsToBuy": true,
        "isUrgent": false
    }
]
```

**Обратная совместимость:**
- Старый формат (без `isUrgent`) сохранен под ключом `"products_list"`
- При первом запуске происходит автоматическая миграция
- После миграции старые данные НЕ удаляются (для безопасности)

**Точки сохранения:**
- После добавления товара
- После удаления товара
- После изменения `needsToBuy`
- После изменения `isUrgent`
- В `onStop()` Activity (дополнительная страховка)

---

## 💻 Технический стек

### Android SDK
- **compileSdk:** 36 (Android 15)
- **targetSdk:** 36 (Android 15)
- **minSdk:** 24 (Android 7.0 Nougat, API 24)
- **versionCode:** 7
- **versionName:** "1.7"

### Язык и компилятор
- **Kotlin:** 2.0.21
- **Java Compatibility:** 11 (source + target)
- **JVM Target:** 11
- **Kotlin Code Style:** Official

### Основные библиотеки

```gradle
dependencies {
    // AndroidX Core
    implementation 'androidx.core:core-ktx:1.10.1'
    implementation 'androidx.appcompat:appcompat:1.6.1'

    // Material Design
    implementation 'com.google.android.material:material:1.10.0'

    // RecyclerView
    implementation 'androidx.recyclerview:recyclerview:1.3.2'

    // ConstraintLayout
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // JSON сериализация
    implementation 'com.google.code.gson:gson:2.10.1'

    // Тестирование
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}
```

### Система сборки
- **Gradle:** 8.13.0
- **Android Gradle Plugin (AGP):** 8.13.0
- **Gradle Version Catalog:** Включен (`libs.versions.toml`)
- **Build Features:**
  - `android.useAndroidX=true`
  - `android.nonTransitiveRClass=true`
  - `minifyEnabled=false` (в debug и release)

### Требования для разработки
- **Java:** JDK 11 или новее (рекомендуется JDK 21)
- **Android Studio:** Hedgehog | 2023.1.1 или новее
- **Gradle Wrapper:** Включен в проект (используйте `./gradlew`)

---

## 📐 Соглашения по коду

### Стиль кода

**Kotlin Code Style:** Official (настроен в `gradle.properties`)

**Основные правила:**
- 4 пробела для отступов (НЕ табы)
- Максимальная длина строки: 120 символов
- Фигурные скобки на той же строке (K&R style)
- Пустая строка между методами
- Imports сортируются автоматически

**Пример:**
```kotlin
class ProductManager(private val dataManager: DataManager) {
    private val _products = mutableListOf<Product>()

    fun addProduct(name: String, isUrgent: Boolean = false): Boolean {
        if (_products.any { it.name.equals(name, ignoreCase = true) }) {
            return false
        }

        val product = Product(
            name = name,
            needsToBuy = true,
            isUrgent = isUrgent
        )
        _products.add(product)
        return true
    }
}
```

### Именование

**Classes:** PascalCase
```kotlin
class ProductAdapter
class DataManager
enum class ProductGroup
```

**Functions/Methods:** camelCase (глаголы)
```kotlin
fun addProduct()
fun getGroupedList()
fun toggleProductUrgency()
```

**Properties:** camelCase
```kotlin
val productName: String
var needsToBuy: Boolean
private val _products: MutableList<Product>
```

**Constants:** UPPER_SNAKE_CASE
```kotlin
const val TYPE_GROUP_HEADER = 0
const val TYPE_PRODUCT = 1
private const val PREFS_NAME = "shopping_list_prefs"
```

**Resources:** snake_case
```xml
activity_main.xml
product_item.xml
urgent_background_rounded.xml
ic_star_filled.xml
```

### Логирование

**Tag формат:** `"COMPONENT_NAME"`

```kotlin
// Примеры из кода
Log.d("PRODUCT_GROUP", "=== ГРУППИРОВКА: $name ===")
Log.d("DIFF_UTIL", "areItemsTheSame: $oldItem vs $newItem")
Log.d("ADAPTER", "onBindViewHolder: position=$position")
```

**⚠️ В production версии логи НЕ должны быть видимы!**
Перед релизом убедитесь, что все `Log.d()` обернуты в `if (BuildConfig.DEBUG)` или удалены.

### XML Resources

**Layout naming:**
- `activity_*.xml` — Activities
- `fragment_*.xml` — Fragments (если будут)
- `dialog_*.xml` — Диалоги
- `*_item.xml` — RecyclerView items

**Drawable naming:**
- `ic_*.xml` — Icons
- `bg_*.xml` — Backgrounds
- `*_background_*.xml` — Background resources

**Атрибуты в XML:** Порядок важен!
```xml
<TextView
    android:id="@+id/productName"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/app_name"
    android:textSize="16sp"
    android:textColor="@color/black"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent" />
```

Порядок: id → layout → text/src → style/appearance → constraints

---

## 🔄 Рабочие процессы

### Процесс разработки новой фичи

1. **Создание ветки:**
   ```bash
   git checkout -b claude/feature-name-SESSION_ID
   ```

2. **Анализ требований:**
   - Определить, какие компоненты затронуты
   - Проверить совместимость с существующей архитектурой
   - Учесть особенности MIUI/Xiaomi устройств

3. **Реализация:**
   - Начать с модели данных (`Product.kt` если нужно)
   - Обновить бизнес-логику (`ProductManager.kt`)
   - Модифицировать UI (`MainActivity.kt`, layouts)
   - Обновить адаптер если изменилась визуализация

4. **Тестирование:**
   - Проверить на эмуляторе (желательно с MIUI)
   - Убедиться в корректности сохранения данных
   - Проверить все сценарии группировки
   - Проверить swipe-to-delete, клики, долгие нажатия

5. **Коммит:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   ```

6. **Пуш:**
   ```bash
   git push -u origin claude/feature-name-SESSION_ID
   ```

### Процесс исправления бага

1. **Воспроизведение:**
   - Определить шаги для воспроизведения
   - Проверить логи (если логирование включено)
   - Определить затронутые компоненты

2. **Локализация:**
   - Найти проблемный код
   - Понять root cause (не симптом!)
   - Проверить связанные участки кода

3. **Исправление:**
   - Минимальное изменение для устранения бага
   - НЕ рефакторить одновременно с фиксом
   - Добавить комментарий с объяснением

4. **Регрессионное тестирование:**
   - Проверить, что баг исправлен
   - Убедиться, что ничего не сломалось
   - Проверить edge cases

5. **Документирование:**
   - Обновить комментарии в коде
   - Если баг был критический, добавить в CLAUDE.md в раздел "Подводные камни"

### Процесс рефакторинга

**⚠️ ВАЖНО:** Рефакторинг должен быть отдельным от функциональных изменений!

1. **Определение цели:**
   - Что улучшаем? (читаемость, производительность, архитектуру)
   - Есть ли риски?

2. **Планирование:**
   - Разбить на маленькие шаги
   - Каждый шаг должен компилироваться

3. **Выполнение:**
   - Один рефакторинг за раз
   - Коммит после каждого успешного шага
   - Постоянное тестирование

4. **Валидация:**
   - Функциональность идентична
   - Производительность не ухудшилась
   - Код стал читабельнее

---

## 🌿 Git правила

### Структура веток

**Main branch:** `master` (или `main`)
**Feature branches:** `claude/feature-name-SESSION_ID`
**Session ID:** Уникальный идентификатор сессии (например, `01J7BF7WDrYKiknCVQ7gz9Wa`)

**Текущая рабочая ветка:**
```
claude/add-claude-documentation-01J7BF7WDrYKiknCVQ7gz9Wa
```

### Правила коммитов

**Формат сообщения:**
```
<type>: <subject>

<body> (опционально)
```

**Types:**
- `feat:` — Новая функциональность
- `fix:` — Исправление бага
- `refactor:` — Рефакторинг кода
- `docs:` — Изменения в документации
- `style:` — Форматирование, отступы (не CSS!)
- `test:` — Добавление/изменение тестов
- `chore:` — Обновление зависимостей, конфигурации

**Примеры:**
```bash
git commit -m "fix: Корректное определение группы для товаров в состоянии isUrgent=true && needsToBuy=false"

git commit -m "feat: Добавлена трехуровневая визуальная дифференциация групп товаров"

git commit -m "refactor: Упрощена логика определения фона карточки в ProductAdapter"

git commit -m "docs: Обновлен CLAUDE.md с детальным описанием архитектуры"
```

**Плохие примеры (НЕ делать так!):**
```bash
git commit -m "fix"
git commit -m "updated files"
git commit -m "changes"
```

### Push правила

**⚠️ КРИТИЧЕСКИ ВАЖНО:**

1. **Всегда используйте `-u origin <branch-name>`:**
   ```bash
   git push -u origin claude/feature-name-SESSION_ID
   ```

2. **Имя ветки ДОЛЖНО начинаться с `claude/` и заканчиваться на SESSION_ID!**
   - Иначе получите `403 HTTP error`

3. **При network errors — retry с exponential backoff:**
   - 1-я попытка: немедленно
   - 2-я попытка: через 2 секунды
   - 3-я попытка: через 4 секунды
   - 4-я попытка: через 8 секунд
   - 5-я попытка: через 16 секунд

**Пример bash команды с retry:**
```bash
for i in 1 2 4 8 16; do
    git push -u origin claude/add-claude-documentation-01J7BF7WDrYKiknCVQ7gz9Wa && break || sleep $i
done
```

### Fetch/Pull правила

**Предпочтительно fetching конкретной ветки:**
```bash
git fetch origin claude/feature-name-SESSION_ID
```

**Для pull:**
```bash
git pull origin claude/feature-name-SESSION_ID
```

**⚠️ Также применяется retry логика при network failures!**

---

## 🛠️ Частые задачи

### Добавление нового поля в Product

**Шаги:**

1. **Обновить модель данных:**
   ```kotlin
   // Product.kt
   data class Product(
       val name: String,
       var needsToBuy: Boolean = false,
       var isUrgent: Boolean = false,
       var newField: String = ""  // Новое поле
   )
   ```

2. **Обновить DataManager для backward compatibility:**
   ```kotlin
   // DataManager.kt
   private data class OldProduct(
       val name: String,
       var needsToBuy: Boolean = false,
       var isUrgent: Boolean = false
       // newField отсутствует в старой версии
   )

   // При загрузке преобразовать:
   val products = oldProducts.map {
       Product(it.name, it.needsToBuy, it.isUrgent, newField = "")
   }
   ```

3. **Обновить UI в ProductAdapter (если нужно отображение):**
   ```kotlin
   // ProductAdapter.kt - ProductViewHolder
   holder.newFieldTextView.text = product.newField
   ```

4. **Обновить DiffUtil:**
   ```kotlin
   override fun areContentsTheSame(oldItem: Any, newItem: Any): Boolean {
       return when {
           oldItem is Product && newItem is Product -> {
               oldItem.name == newItem.name &&
               oldItem.needsToBuy == newItem.needsToBuy &&
               oldItem.isUrgent == newItem.isUrgent &&
               oldItem.newField == newItem.newField  // Добавить
           }
           else -> false
       }
   }
   ```

5. **Тестирование:**
   - Установить старую версию, добавить данные
   - Установить новую версию, проверить миграцию
   - Убедиться, что данные сохраняются корректно

### Добавление нового типа группы

**⚠️ Это сложная задача, требующая изменений во всей архитектуре!**

**Затронутые компоненты:**
1. `Product.kt` — добавить новое значение в `enum class ProductGroup`
2. `Product.kt` — обновить логику `getGroup()`
3. `ProductAdapter.kt` — добавить новую визуализацию
4. `ProductManager.kt` — обновить порядок групп в `getGroupedList()`
5. `res/values/strings.xml` — добавить название группы
6. `res/drawable/` — создать новый background и иконки
7. Тестирование всех сценариев

**Рекомендация:** НЕ добавлять новые группы без крайней необходимости!

### Изменение визуализации группы

**Например, изменить цвет фона для группы URGENT_TO_BUY:**

1. **Найти drawable:**
   ```
   app/src/main/res/drawable/urgent_background_rounded.xml
   ```

2. **Изменить цвета в gradient:**
   ```xml
   <gradient
       android:startColor="#FFE5F0"  <!-- Изменить -->
       android:endColor="#FFB3D9"    <!-- Изменить -->
       android:angle="135" />
   ```

3. **НЕ трогать логику в ProductAdapter!**
   Визуализация привязана к группе, не к флагам.

4. **Пересобрать и протестировать:**
   ```bash
   ./gradlew assembleDebug
   ```

### Добавление нового элемента в карточку товара

**Пример: добавить TextView для даты добавления**

1. **Обновить layout:**
   ```xml
   <!-- product_item.xml -->
   <TextView
       android:id="@+id/dateAdded"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:text="2024-11-18"
       android:textSize="12sp"
       app:layout_constraintTop_toBottomOf="@id/productName"
       app:layout_constraintStart_toStartOf="parent" />
   ```

2. **Обновить ViewHolder:**
   ```kotlin
   // ProductAdapter.kt
   inner class ProductViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
       val urgentStar: ImageView = itemView.findViewById(R.id.urgentStar)
       val productName: TextView = itemView.findViewById(R.id.productName)
       val switchBuy: SwitchMaterial = itemView.findViewById(R.id.switchBuy)
       val dateAdded: TextView = itemView.findViewById(R.id.dateAdded)  // Новое
   }
   ```

3. **Обновить onBindViewHolder:**
   ```kotlin
   holder.dateAdded.text = product.dateAdded
   ```

4. **Обновить DiffUtil (см. выше "Добавление нового поля в Product")**

---

## ⚠️ Подводные камни

### 1. Баг #13: Неправильное определение фона карточки

**Проблема:**
Фон карточки определялся напрямую по флагу `isUrgent`, а не по группе `product.getGroup()`.

**Симптомы:**
Товар с `isUrgent=true && needsToBuy=false` (невалидное состояние) показывался с розовым фоном в группе "Остальное".

**Решение:**
```kotlin
// ❌ ПЛОХО (старый код):
val backgroundRes = if (product.isUrgent) {
    R.drawable.urgent_background_rounded
} else {
    R.drawable.product_item_background_white
}

// ✅ ХОРОШО (исправленный код):
val backgroundRes = when (product.getGroup()) {
    ProductGroup.URGENT_TO_BUY -> R.drawable.urgent_background_rounded
    ProductGroup.TO_BUY -> R.drawable.product_item_background_unified
    ProductGroup.OTHER -> R.drawable.product_item_background_white
}
```

**Правило:** Всегда использовать `product.getGroup()` для визуальных решений, а не напрямую флаги!

### 2. MIUI/Xiaomi RecyclerView проблемы

**Проблема:**
На устройствах Xiaomi с MIUI RecyclerView иногда не обновляет визуальные элементы после `notifyItemChanged()`.

**Симптомы:**
- Звездочка не меняет прозрачность
- Фон не обновляется
- Switch не синхронизируется с данными

**Решение:**
Принудительное обновление через `post()`:

```kotlin
holder.itemView.post {
    holder.urgentStar.alpha = when (product.getGroup()) {
        ProductGroup.URGENT_TO_BUY -> 1.0f
        ProductGroup.TO_BUY -> 0.3f
        ProductGroup.OTHER -> 0.1f
    }

    holder.switchBuy.isChecked = product.needsToBuy
}
```

### 3. DiffUtil payloads не работают для вложенных изменений

**Проблема:**
При использовании `payloads` в `onBindViewHolder(holder, position, payloads)`, RecyclerView может пропустить обновление, если payload некорректный.

**Решение:**
Всегда проверять `payloads.isEmpty()` и иметь fallback:

```kotlin
override fun onBindViewHolder(holder: ProductViewHolder, position: Int, payloads: List<Any>) {
    if (payloads.isEmpty()) {
        // Fallback на полное обновление
        super.onBindViewHolder(holder, position, payloads)
    } else {
        // Обработать payload
        when (payloads[0]) {
            "urgency_changed" -> updateUrgencyVisuals(holder, position)
            "visual_changed" -> updateAllVisuals(holder, position)
        }
    }
}
```

### 4. GridLayoutManager SpanSizeLookup и позиции

**Проблема:**
При использовании `SpanSizeLookup` позиции элементов могут не совпадать с визуальной сеткой.

**Симптомы:**
- Клики обрабатываются не на те элементы
- Анимации применяются к неправильным items

**Решение:**
Всегда работать с `position` из адаптера, а не из LayoutManager:

```kotlin
// ✅ ПРАВИЛЬНО:
val item = displayList[position]

// ❌ НЕПРАВИЛЬНО:
val layoutPosition = holder.layoutPosition
val item = displayList[layoutPosition]  // Может быть неверным!
```

### 5. SharedPreferences и конкурентный доступ

**Проблема:**
При быстрых изменениях (например, быстрые клики на звездочки) данные могут не успевать сохраняться.

**Текущее решение:**
Синхронное сохранение в главном потоке (блокирующее, но надежное):

```kotlin
fun saveProducts(products: List<Product>) {
    val json = gson.toJson(products)
    prefs.edit().putString(KEY_PRODUCTS_V2, json).apply()
}
```

**⚠️ НЕ менять на `.commit()` без необходимости!**
`.apply()` работает асинхронно и достаточно быстро для нашего случая.

### 6. Проблемы с Gradle и Java версиями

**Проблема:**
Ошибка `No Java compiler found, please ensure you are running Gradle with a JDK`.

**Причина:**
В системе установлен JRE вместо JDK, или Gradle использует неправильную версию Java.

**Решение:**
См. файл `GRADLE_JAVA_FIX.md`. Кратко:

1. Установить JDK 11 или новее
2. Установить `JAVA_HOME`:
   ```bash
   export JAVA_HOME=/path/to/jdk-11
   export PATH=$JAVA_HOME/bin:$PATH
   ```
3. Использовать gradle wrapper:
   ```bash
   ./gradlew build
   ```

**⚠️ НЕ использовать системный Gradle, всегда использовать wrapper!**

### 7. Обратная совместимость данных

**Проблема:**
После добавления поля `isUrgent` старые данные без этого поля не загружаются.

**Решение:**
Двухключевая система хранения:
- `"products_list"` — старый формат (без `isUrgent`)
- `"products_list_v2"` — новый формат (с `isUrgent`)

При загрузке:
1. Пытаемся загрузить из `v2`
2. Если пусто, загружаем из старого ключа
3. Мигрируем: `isUrgent = false` для всех
4. Сохраняем в `v2`

**⚠️ При добавлении новых полей повторить эту логику!**

---

## 🧪 Тестирование

### Unit тесты

**Расположение:** `app/src/test/java/com/example/shoppinglist/`

**Текущее состояние:**
Только базовый `ExampleUnitTest.kt` (placeholder).

**Рекомендации для тестирования:**

1. **ProductManager:**
   - Добавление товара с дубликатом
   - Группировка товаров
   - Сортировка внутри групп
   - Переключение статусов

2. **Product.getGroup():**
   - Все 4 комбинации флагов
   - Проверка корректности группы

3. **DataManager:**
   - Сохранение и загрузка
   - Миграция старого формата
   - Пустые данные

**Пример теста:**
```kotlin
@Test
fun `product with urgent and needsToBuy should be in URGENT_TO_BUY group`() {
    val product = Product(name = "Test", needsToBuy = true, isUrgent = true)
    assertEquals(ProductGroup.URGENT_TO_BUY, product.getGroup())
}
```

### Instrumented тесты

**Расположение:** `app/src/androidTest/java/com/example/shoppinglist/`

**Текущее состояние:**
Только базовый `ExampleInstrumentedTest.kt`.

**Рекомендации:**

1. **UI тесты:**
   - Добавление товара через FAB
   - Swipe to delete
   - Клик на звездочку
   - Переключение Switch

2. **Espresso пример:**
   ```kotlin
   @Test
   fun addProductDialogOpensOnFabClick() {
       onView(withId(R.id.fab)).perform(click())
       onView(withId(R.id.productNameInput)).check(matches(isDisplayed()))
   }
   ```

### Ручное тестирование

**Обязательные сценарии:**

1. **Добавление товара:**
   - [ ] Обычный товар
   - [ ] Срочный товар (checkbox)
   - [ ] Дубликат (должен отклоняться)
   - [ ] Пустое имя (должен отклоняться)

2. **Переключение срочности:**
   - [ ] Клик на звездочку: `isUrgent=false → true` (автоматически `needsToBuy=true`)
   - [ ] Клик на звездочку: `isUrgent=true → false`
   - [ ] Долгое нажатие (альтернативный способ)
   - [ ] Группа меняется автоматически

3. **Переключение needsToBuy:**
   - [ ] `false → true` (группа TO_BUY или URGENT_TO_BUY)
   - [ ] `true → false` (группа OTHER, `isUrgent` сбрасывается)

4. **Удаление:**
   - [ ] Swipe влево
   - [ ] Swipe вправо
   - [ ] Удаление последнего товара в группе (заголовок исчезает)

5. **Персистентность:**
   - [ ] Закрыть приложение (убить процесс)
   - [ ] Открыть снова
   - [ ] Все данные на месте

6. **Группировка:**
   - [ ] Товары в правильных группах
   - [ ] Порядок групп: СРОЧНО → ВАЖНО → ОСТАЛЬНОЕ
   - [ ] Внутри группы сортировка по алфавиту

7. **Визуализация:**
   - [ ] СРОЧНО: розовый фон, яркая звезда (alpha=1.0)
   - [ ] ВАЖНО: градиент, полупрозрачная звезда (alpha=0.3)
   - [ ] ОСТАЛЬНОЕ: белый фон, бледная звезда (alpha=0.1)

### Тестирование на устройствах

**Приоритетные устройства:**
1. **Xiaomi с MIUI** (критично!)
2. Samsung с One UI
3. Google Pixel (stock Android)
4. Старое устройство на Android 7.0 (minSdk 24)

**Эмулятор:**
- API 24 (минимальная поддержка)
- API 36 (целевая версия)
- MIUI эмулятор (если доступен)

---

## 🚀 Сборка и развертывание

### Команды сборки

**Очистка проекта:**
```bash
./gradlew clean
```

**Debug сборка:**
```bash
./gradlew assembleDebug
```

**Release сборка:**
```bash
./gradlew assembleRelease
```

**Установка на устройство:**
```bash
# Debug
./gradlew installDebug

# Release (требуется подпись!)
./gradlew installRelease
```

**Список устройств:**
```bash
adb devices
```

### Структура APK файлов

**Debug APK:**
```
app/build/outputs/apk/debug/app-debug.apk
```

**Release APK:**
```
app/build/outputs/apk/release/app-release-unsigned.apk
```

**Архив релизов:**
```
releases/shopping-list-v1.5-successful-build-20251102-2254.apk
```

### Версионирование

**Где обновлять версию:**
```gradle
// app/build.gradle
android {
    defaultConfig {
        versionCode 7        // Целое число, увеличивается каждый релиз
        versionName "1.7"    // Строка, отображается пользователю
    }
}
```

**Naming convention для релизов:**
```
shopping-list-v{versionName}-{status}-{date}-{time}.apk
                                       YYYYMMDD  HHMM

Пример:
shopping-list-v1.7-successful-build-20251118-1430.apk
```

### Release checklist

Перед релизом проверить:

- [ ] `versionCode` увеличен
- [ ] `versionName` обновлен
- [ ] Все логи удалены или обернуты в `if (BuildConfig.DEBUG)`
- [ ] Нет TODO в production коде
- [ ] Все тесты проходят
- [ ] Ручное тестирование выполнено
- [ ] README.md обновлен
- [ ] CHANGELOG добавлен (если есть)
- [ ] APK подписан (для production)
- [ ] ProGuard настроен (если `minifyEnabled = true`)

### ProGuard (если будет использоваться)

**Текущее состояние:** `minifyEnabled = false`

**Если включить minify, добавить в `proguard-rules.pro`:**
```proguard
# Gson
-keepattributes Signature
-keepattributes *Annotation*
-keep class com.example.shoppinglist.Product { *; }
-keep class com.example.shoppinglist.ProductGroup { *; }

# Material Components
-keep class com.google.android.material.** { *; }
```

---

## 📚 Дополнительные ресурсы

### Документация проекта

- `README.md` — Основная документация (русский)
- `CLAUDE.md` — Этот файл (для AI ассистентов)
- `GRADLE_JAVA_FIX.md` — Решение проблем с Gradle и Java
- `URGENCY_VISUAL_FIX_INSTRUCTION.md` — История визуальных фиксов
- `PROJECT_OPTIMIZATION_COMPLETE.md` — История оптимизации

### Внешние ресурсы

**Android:**
- [Android Developer Guide](https://developer.android.com/guide)
- [Material Design 3](https://m3.material.io/)
- [RecyclerView Guide](https://developer.android.com/guide/topics/ui/layout/recyclerview)
- [DiffUtil Documentation](https://developer.android.com/reference/androidx/recyclerview/widget/DiffUtil)

**Kotlin:**
- [Kotlin Documentation](https://kotlinlang.org/docs/home.html)
- [Kotlin Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html)

**Gradle:**
- [Gradle User Guide](https://docs.gradle.org/current/userguide/userguide.html)
- [Android Gradle Plugin](https://developer.android.com/build)

---

## 🎯 Ключевые принципы для AI ассистентов

### При работе с этим проектом ВСЕГДА:

1. **Используйте `product.getGroup()` для визуальных решений**, а не напрямую флаги `isUrgent`/`needsToBuy`

2. **Сохраняйте данные после каждого изменения** через `dataManager.saveProducts()`

3. **Используйте DiffUtil с payloads** для оптимизации RecyclerView обновлений

4. **Учитывайте особенности MIUI/Xiaomi** — используйте `post()` для принудительного обновления

5. **Поддерживайте обратную совместимость данных** при добавлении новых полей

6. **Следуйте Kotlin Official Code Style** (4 пробела, K&R braces)

7. **Пишите commit messages на русском** с правильным форматом `<type>: <subject>`

8. **Используйте `./gradlew`** (wrapper), а не системный Gradle

9. **Тестируйте на эмуляторе** перед коммитом (минимум API 24)

10. **Обновляйте этот CLAUDE.md** при изменении архитектуры или добавлении важных концепций

### При возникновении проблем:

1. Проверьте раздел "Подводные камни" в этом документе
2. Проверьте логи Android (если логирование включено)
3. Проверьте, что используете правильную версию Java (JDK 11+)
4. Проверьте, что ветка соответствует формату `claude/*-SESSION_ID`
5. Проверьте файл `GRADLE_JAVA_FIX.md` для проблем сборки

---

## 📝 История изменений

**v1.7 (2025-11-18):**
- Создан comprehensive CLAUDE.md для AI ассистентов
- Документирована трехуровневая группировка
- Описаны все подводные камни и решения
- Добавлены инструкции по Git workflow

**v1.5-1.6:**
- Исправлен баг #13 с группировкой
- Оптимизация для MIUI
- Улучшена визуализация звездочек
- Единый стиль карточек

**v1.0-1.4:**
- Базовая реализация приложения
- Добавлена группировка товаров
- Реализовано сохранение данных
- Material Design интеграция

---

**🎉 Этот документ является живым и должен обновляться при значительных изменениях в проекте!**

---

*Последнее обновление: 2025-11-18 | Автор: Claude AI Assistant*
